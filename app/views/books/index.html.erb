
<%= render 'users/list', book: @book, user: @user %>

<h1>Books</h1>
<table>
<tr>
  <th>title</th>
  <th>opinion</th>
</tr>
  <% @books.each do |book| %>
  <tr>
    <td><%= link_to book.title, book_path(book.id) %></td>
    <td><%= book.body %></td>
  </tr>
  <% end %>
  </table>