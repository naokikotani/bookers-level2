<%= render 'users/list', book: @book, user: @user %>

<h1>Users</h1>
<table>
<tr>
  <th>image</th>
  <th>name</th>
</tr>
<% @users.each do |user| %>
<tr>
  <td><%= user.name %></td>
  <td><%= link_to "show", user_path(user.id) %></td>
</tr>
<% end %>
</table>